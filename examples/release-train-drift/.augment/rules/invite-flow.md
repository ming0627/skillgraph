---
type: agent_requested
description: Apply when changing workspace invite behavior.
---

# Augment Invite Flow Rule

Use implementation-workflow before editing invite code.

Use auth-qa and browser-qa before saying the protected flow is ready.

Use review-gate for code review and release-gate for release handoff.
