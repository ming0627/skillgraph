# Factory Invite Flow Rule

Factory Droids should use implementation-workflow before editing invite code.

Use auth-qa and browser-qa before marking protected routes ready.

Use review-gate, release-gate, and rollback-runbook for release handoff.
