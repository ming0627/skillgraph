# Kilo Invite Flow Rules

Use implementation-workflow for invite edits.

Use auth-qa and browser-qa before marking protected invite routes ready.

Use review-gate and release-gate before final output.
